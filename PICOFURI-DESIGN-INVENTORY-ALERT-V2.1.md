# 在庫アラート機能 設計書 v2.1

> 作成日: 2026-04-15
> ステータス: 設計レビュー中
> 前版からの変更: トレンド分析、欠品期間除外、リードタイム、通知疲れ防止を追加

---

## 1. 目的

1. **欠品防止** — 在庫が危険水準まで減った商品を早期に検知
2. **仕入れ判断** — 在庫日数・販売トレンドから補充の優先度を判断
3. **価格変更示唆** — 販売ペースが減速した商品を検知
4. **フリマ監視の追加推奨** — 売れ筋なのにフリマ未監視の商品を提案

---

## 2. 対象商品の条件

CROSSMALL全商品のうち、以下をすべて満たすもの：

- `item_code` が `2314-` で始まる（N2M取扱商品）
- 以下のいずれかに該当：
  - `crossmall_sales_history.json` に販売実績がある
  - `purchase_master_cache.json` で `stock >= 1`

※ 在庫0かつ販売実績ゼロの商品は対象外（両方ゼロ = 存在しないに等しい）

---

## 3. 核心ロジック

### 3-A. 販売ペース計算（欠品期間を除外）

28日間で在庫0だった日が10日あった場合、その10日は「売れなかった」のではなく「売れなかった」。
正しい実力値を出すため、在庫があった日数で割る。

```
実効販売日数 = 対象期間 − 欠品日数
実効日販 = 対象期間の販売数 ÷ 実効販売日数
```

| 期間 | 販売数 | 暦日数 | 欠品日 | 実効日数 | 実効日販 |
|------|:------:|:------:|:------:|:-------:|:-------:|
| 28日 | 14個 | 28 | 10 | 18 | 0.78個/日 |
| 14日 | 10個 | 14 | 3 | 11 | 0.91個/日 |
| 7日 | 7個 | 7 | 0 | 7 | 1.00個/日 |

※ 欠品日数の判定: `crossmall_sales_history.json` に販売記録がなく、かつ前後の同期でstock=0だった日
※ 初期実装では簡易版として「在庫同期時点でstock=0なら欠品中」と判定（日単位の厳密な追跡は後回し）

**実効日数が0の場合（全日欠品）: 日販を0とし「全日欠品」と表示**

### 3-B. 在庫日数の計算（3期間の最短予測）

3期間それぞれで在庫日数を計算し、**最も短いもの**を採用。
直近の加速を見逃さないため。

```
在庫日数_28d = stock ÷ 実効日販_28d
在庫日数_14d = stock ÷ 実効日販_14d
在庫日数_7d  = stock ÷ 実効日販_7d

採用在庫日数 = MIN(在庫日数_28d, 在庫日数_14d, 在庫日数_7d)
```

例: 在庫50個、28日日販1.0、14日日販1.5、7日日販3.0の場合
→ 在庫日数は50÷3.0=16.7日（7日ベースが最短 → 加速中）

### 3-C. 販売トレンド判定

28日と7日の実効日販を比較してトレンドを判定。

```
加速率 = 実効日販_7d ÷ 実効日販_28d
```

| トレンド | 条件 | 意味 |
|---------|------|------|
| 📈 加速 | 加速率 ≥ 1.5 | 最近売れ始めた。仕入れ急ぐ |
| ➡️ 安定 | 0.5 < 加速率 < 1.5 | 安定ペース |
| 📉 減速 | 加速率 ≤ 0.5 | 売れなくなった。**価格変更を検討** |
| ⚡ 新規急伸 | 28日日販 < 0.3 かつ 7日日販 ≥ 1.0 | 今まで売れなかったのに急に売れだした |
| ⏸ 判定不能 | 全期間販売ゼロ or 実効日数不足 | データ不足 |

### 3-D. リードタイム（補充所要日数）

在庫日数がアラート閾値を満たしていても、補充に時間がかかるなら手遅れ。

```
有効残日数 = 採用在庫日数 − リードタイム
```

| 設定 | 値 | 備考 |
|------|-----|------|
| デフォルトリードタイム | 3日 | 全SKU共通の初期値 |
| SKU個別設定 | 未実装（将来Telegramから設定可能に） | Phase 2以降 |

**アラート判定には「有効残日数」を使用:**

| レベル | 条件（有効残日数ベース） |
|--------|----------------------|
| 🔴 危険 | 有効残日数 ≤ 0日（もう間に合わない） |
| 🟡 注意 | 有効残日数 1〜4日 |
| 🟢 余裕 | 有効残日数 5日以上 |

例: 在庫日数5日、リードタイム3日 → 有効残日数2日 → 🟡注意
例: 在庫日数2日、リードタイム3日 → 有効残日数-1日 → 🔴危険（すでに手遅れ）

---

## 4. 分類（6段階）

| レベル | 条件 | アクション |
|--------|------|-----------|
| 🔴📈 仕入れ急げ | 有効残日数≤0 + 加速中 | 即発注 |
| 🔴 危険 | 有効残日数≤0 | 早急に補充判断 |
| 🟡 注意 | 有効残日数1〜4日 | 補充準備 |
| 🟢 余裕 | 有効残日数5日以上 | 対応不要 |
| ⚫ 欠品中 | stock=0 + 販売実績あり | 即補充 or 廃盤判断 |
| 💰 価格見直し | 加速率≤0.5 + stock>0 | 販促・値下げ検討 |
| ⚪ 不動在庫 | stock>0 + 全期間販売ゼロ | 処分検討 |

---

## 5. 通知設計

### 5-A. 毎朝サマリー（LINE + Telegram / 8:00 JST）

**通知疲れ防止ルール:**
- **新規・悪化のみ強調表示**（前日と同じ状態の商品は省略 or 件数のみ）
- 各カテゴリに「新規X件 / 継続X件」を明記
- LINE版は各カテゴリ上位5件のみ、Telegram版は全件展開可能

```
📊 在庫日報 2026-04-16（リードタイム: 3日）

🔴📈 仕入れ急げ（加速中＋残0日以下）: 2件 [NEW 1件]
  🆕 メディカルブレス（残3個/7日3.0個/日📈/有効残-2日）
  　 蓬緑（残2個/7日1.5個/日📈/有効残-1日）

⚫ 欠品中: 3件 [NEW 0件]
  ニコせっけん（0個/実力0.8個/日/最終4/14/5日前）
  オキシカット（0個/実力0.3個/日/最終4/10/⚠️低速）
  レムウェル（0個/実力0.1個/日/最終3/28/⚠️19日前→廃盤？）

🔴 危険: 3件 [NEW 1件]
  🆕 商品A（残5個/有効残0日/安定）
  　 商品B（残4個/有効残-1日/安定）
  　 他1件

💰 価格見直し: 4件 [NEW 2件]
  🆕 商品C（残30個/28日1.5個→7日0.1個📉/急減速）
  🆕 商品D（残15個/28日0.8個→7日0個📉/停止）
  　 他2件

🟡 注意: 8件 [NEW 2件 / 継続6件]
🟢 余裕: XXX件
⚪ 不動在庫: XX件

📋 全体: 対象XXX件 / 🔴X / 🟡X / 🟢X / ⚫X / 💰X / ⚪X
前日比: 🔴+1 🟡-2（改善2件・悪化1件）

💡 フリマ監視おすすめ（売れ筋で未監視）: 3件
  商品X（28日25個/在庫30個/未監視）
  商品Y（7日日販1.4📈/在庫50個/未監視/新トレンド）
```

### 5-B. 2時間チェック（Telegramのみ）

**タイミング:** crossmall-sync完了後
**条件:** 🔴 または ⚫ が1件以上ある場合のみ
**通知疲れ防止:** 前回送信時と内容が同じなら送信しない

```
⚠️ 在庫アラート（14:00更新）
🔴2件 ⚫3件
→ メディカルブレス 残3個(有効残-2日)📈加速中
→ 蓬緑 残2個(有効残-1日)📈
→ ニコせっけん 欠品(実力0.8個/日)
→ オキシカット 欠品(低速)
→ レムウェル 欠品(19日前→廃盤？)
```

### 5-C. 急減アラート（LINE + Telegram / 即時）

**条件:** 前回sync比で在庫が50%以上減少 かつ 有効残日数4日以下になった場合
**通知疲れ防止:** 同一商品の急減アラートは24時間に1回まで

```
🚨 在庫急減アラート
メディカルブレス: 15個→3個（-80%）
  7日日販3.0個📈（加速中）有効残-2日
  → 即発注推奨
```

---

## 6. フリマ監視 追加推奨

### 判断基準

**パターンA: 売れ筋なのに未監視**
- 28日販売数 ≥ 10個
- フリマ監視キーワードにSKU未紐付け

**パターンB: 在庫減少中なのに未監視**
- 有効残日数 ≤ 14日
- 28日販売数 ≥ 1個
- フリマ監視キーワードにSKU未紐付け

**パターンC: 新規急伸なのに未監視**
- トレンド = ⚡新規急伸
- フリマ監視キーワードにSKU未紐付け

### 通知タイミング
毎朝サマリーの末尾に追加。
Telegram botの➕追加で即座に監視開始可能。

---

## 7. データフロー

```
crossmall-sync（2時間ごと）
  ↓
  ├→ get_order → sales history 更新
  ├→ get_stock → 在庫数更新
  ├→ get_item → 商品名更新（差分のみ）
  ├→ computeStats() → sales28/14/7, lastSaleDate
  ├→ cache保存 + POST /api/cache/reload
  ↓
  ↓ 【在庫アラートチェック】
  ↓   1. 前回状態を読み込み（inventory_alert_state.json）
  ↓   2. 全対象SKUの在庫日数・トレンドを計算
  ↓   3. 急減チェック（前回比50%以上減 + 有効残4日以下）
  ↓   4. 現在状態を保存（次回比較用）
  ↓
  ├→ 急減あり → LINE + Telegram即時送信
  ├→ 🔴/⚫あり → Telegram簡易チェック送信（前回と異なる場合のみ）
  └→ 完了

inventory-alert（毎朝8:00 JST、PM2 cron）
  ↓ inventory_alert_state.json を読み込み
  ↓ 前日状態と比較 → 新規/悪化/改善を判定
  ↓ フリマ推奨リスト生成
  ├→ LINE pushMessage（サマリー）
  └→ Telegram sendMessage（サマリー）
```

---

## 8. ファイル構成

### 新規ファイル
| ファイル | 役割 |
|---------|------|
| `server/services/InventoryAlertService.js` | 在庫日数・トレンド計算・分類・推奨ロジック |
| `server/scripts/inventory-alert.cjs` | 毎朝8時のサマリー送信（PM2 cron） |
| `server/data/inventory_alert_state.json` | 前回の在庫状態（急減比較 + 通知疲れ防止） |
| `server/data/inventory_alert_history.json` | 前日の分類結果（毎朝比較用） |

### 既存ファイルの修正
| ファイル | 変更内容 |
|---------|---------|
| `sync-crossmall-prices.cjs` | sync完了後にアラートチェック追加 |
| `telegram-bot.cjs` | 📦在庫アラート ボタン追加（手動確認用） |

### PM2登録
```bash
pm2 start server/scripts/inventory-alert.cjs \
  --name inventory-alert \
  --cron "0 23 * * *" \
  --no-autorestart
# ※ UTC 23:00 = JST 翌08:00
pm2 save
```

---

## 9. 欠品日数の追跡（簡易版）

### 初期実装（Phase 1）
- crossmall-sync実行時に `stock=0` のSKUを記録
- `inventory_alert_state.json` に `outOfStockSince: { "SKU": "2026-04-10T..." }` で管理
- 欠品日数 = 現在 − outOfStockSince
- 在庫が入ったら outOfStockSince を削除

### 販売ペース補正の簡易計算
```
補正済み実効日販_28d = sales28 ÷ (28 − 欠品日数_28d期間内)
```
※ 欠品日数が28日中20日以上の場合は「データ不足」扱い

---

## 10. 設定値

| パラメータ | 初期値 | 変更方法 |
|-----------|--------|---------|
| デフォルトリードタイム | 3日 | .env `DEFAULT_LEAD_TIME=3` |
| 加速判定閾値 | 1.5倍 | InventoryAlertService内定数 |
| 減速判定閾値 | 0.5倍 | InventoryAlertService内定数 |
| 急減アラート閾値 | 50%減 | InventoryAlertService内定数 |
| 急減アラート抑制 | 24時間/商品 | inventory_alert_state.json |
| 毎朝サマリー時刻 | 08:00 JST | PM2 cron |
| 2時間チェック | sync完了後 | sync-crossmall-prices.cjs |

---

## 11. 段階的な実装計画

### Phase 1: 基盤（まずこれだけ動かす）
- InventoryAlertService.js: 在庫日数計算（3期間最短）+ トレンド判定 + 欠品期間簡易追跡
- inventory-alert.cjs: 毎朝8時のLINE + Telegramサマリー送信
- inventory_alert_state.json: 状態保存
- PM2登録
- リードタイムはデフォルト3日固定

### Phase 2: リアルタイム通知 + 通知疲れ防止
- sync-crossmall-prices.cjs に急減チェック追加
- 2時間ごとのTelegram簡易チェック（変化時のみ）
- 通知疲れ防止（新規/悪化のみ強調、同一内容スキップ、24時間抑制）
- inventory_alert_history.json: 前日比較

### Phase 3: フリマ推奨 + Telegram拡張
- SKU紐付けチェック → 未監視売れ筋の提案
- Telegram botに📦ボタン追加
- SKU個別のリードタイム設定

### Phase 4: 精度向上（将来）
- 曜日・給料日パターン分析
- セット品・同一商品の別SKU統合ビュー
- 欠品日数の日次厳密追跡

---

## 12. 実装前の確認事項

1. **2314-*の全商品数を把握**
   ```bash
   # purchase_master_cache + crossmall_sales_history から2314-*を集計
   ```

2. **crossmall_sales_history.json のデータ構造で14日/7日の販売数を計算可能か確認**
   - 現在 computeStats() が sales28/sales7 を計算
   - sales14 の追加が必要

3. **stock=0の日を追跡するデータが現在あるか確認**
   - なければ初回sync時点からの記録開始（過去分は補正なし）
